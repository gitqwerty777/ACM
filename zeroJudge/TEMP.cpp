#include <iostream>
#include <sstream>
using namespace std;

int main(){
	int x,y,z,n,w,m;
	string eat;
	int i, j, count, lo;
	stringstream stream;
	
	cin>>t;
	bool p;
	int ans;
	while(cin>>x>>y>>z>>w>>n>>m){
		getline(cin, eat);
		lo = eat.size();
		
		if(m > 0)
			for(i = 0; i < lo; i++)
				if(isdigit())
		else
			cout<<"bye~Rabbit"<<endl;
		
		
	}

	return 0;
}
