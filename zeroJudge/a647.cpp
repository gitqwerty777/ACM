#include <iostream>

using namespace std;

int main(){
	int m, p;
	string eat;
	int i, j, count, lo;
	stringstream stream;
	
	bool p;
	int ans;
	
	cin>>t;
	
	for(k = 0; k < t; k++){
		cin>>m>>p;
		lo = eat.size();
		
		if(m > 0)
			for(i = 0; i < lo; i++)
				if(isdigit())
		else
			cout<<"bye~Rabbit"<<endl;
		
		
	}

	return 0;
}
