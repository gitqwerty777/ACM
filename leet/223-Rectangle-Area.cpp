class Point{
  int x, y;
  Point(int x, int y): x(x), y(y);
};

class Solution {
public:
  int computeArea(int A, int B, int C, int D, int E, int F, int G, int H) {
    int AreaA = computeSquareArea(A, B, C, D);
    int AreaB = computeSquareArea(E, F, G, H);
    //find intersections
    int AreaC = 
  }

  int 
};
